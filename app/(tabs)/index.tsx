import {Text} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

function HomeScreen() {
  return (
      <SafeAreaView>
          <Text className={'text-2xl font-black text-red-400'}>Hello Testing</Text>
          <Text className={''}></Text>
      </SafeAreaView>
  );
}

export default HomeScreen;
